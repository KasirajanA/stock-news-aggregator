# Core Django (minimal)
Django==4.2.7
djangorestframework==3.14.0
django-cors-headers==4.3.1

# Database
django-filter==23.3

# Minimal text processing (no heavy dependencies)
textblob==0.17.1

# HTTP requests (minimal)
requests==2.31.0

# Environment
python-dotenv==1.0.0 